import CustomAdminMenu from './CustomAdminMenu';
import CustomSettings from './CustomSettings';
import CustomStatus from './CustomStatus';

export default {
    menus: [CustomAdminMenu],
    status: [CustomStatus],
    settings: [CustomSettings]
}

export const Items = {
    menus: [CustomAdminMenu],
    status: [CustomStatus],
    settings: [CustomSettings]
}
